name: Deploy Frontend to GitHub Pages

on:
  push:
    branches:
      - main  # سيتم التشغيل عند التحديث في فرع main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/checkout@v4
        with:
          node-version: '18'

      - name: Install and Build Frontend
        run: |
          cd frontend
          npm install
          npm run build

      - name: Upload production-ready static files
        uses: actions/upload-pages-artifact@v3
        with:
          # تأكد أن مجلد المخرجات هو dist أو build حسب مشروعك
          path: ./frontend/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
